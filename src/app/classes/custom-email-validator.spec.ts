import { CustomEmailValidator } from './custom-email-validator';

describe('CustomEmailValidator', () => {
  it('should create an instance', () => {
    expect(new CustomEmailValidator()).toBeTruthy();
  });
});
